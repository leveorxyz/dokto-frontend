import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Inter",
    body: "Inter",
  },
  colors: {
    brand: {
      dark: "#26004d",
    },
  },
});

export default theme;
